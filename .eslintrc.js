/* module.exports = {
  extends: [
    'airbnb',  // This extends the Airbnb configuration
    'plugin:react/recommended',  // React-specific linting rules
    'plugin:react-hooks/recommended',  // React Hooks linting rules
  ],
  plugins: [
    'react', // React plugin
    'react-hooks', // React Hooks plugin
  ],
  rules: {
    // Your custom rules can go here
  },
  env: {
    browser: true,  // Specify environment if needed
    es6: true,
    node: true,
  },
  parserOptions: {
    ecmaVersion: 2020,  // Ensure the latest ECMAScript version
    sourceType: 'module',  // If you're using ES modules
    ecmaFeatures: {
      jsx: true,  // Enable JSX parsing
    },
  },
};
 */